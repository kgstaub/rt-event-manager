# GitHub Pages

Promo site for RT Event Manager. Enable via **Settings → Pages → Deploy from a branch → `main` / `docs`**.

Drop screenshots into `docs/img/` using the filenames referenced in `index.html`.
